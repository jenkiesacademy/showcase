{
  "name": "showcase",
  "version": "1.0.0",
  "description": "Engineering Showcase Portfolio - 15 years of software engineering experience",
  "main": "index.js",
  "repository": "https://github.com/jenkies/showcase.git",
  "author": "Jenkies",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "yarn workspaces foreach run dev",
    "build": "yarn workspaces foreach run build",
    "test": "yarn workspaces foreach run test",
    "lint": "yarn workspaces foreach run lint",
    "clean": "yarn workspaces foreach run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=1.22.0"
  }
}

